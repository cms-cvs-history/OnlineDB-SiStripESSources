process test_FedCablingBuilderFromDb = {
    
    include "DQM/SiStripCommon/data/MessageLogger.cfi"
    
    include "IORawData/SiStripInputSources/data/EmptySource.cfi"
    replace EmptySource.maxEvents = 2
    
    module test = test_FedCablingBuilderFromDb { 
	untracked bool UsingDb = true
	untracked string ConfDb = "cms_testBeam/May2004@cmsr"
	untracked string Partition = "TIBSLICE4"
	untracked uint32 MajorVersion = 0
	untracked uint32 MinorVersion = 0
	//untracked string CablingSource = "DEVICES"
    }

    path p = { test }
    
}
